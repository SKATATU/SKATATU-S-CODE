
a = eval(input("Please enter the first number:"))
b = eval(input("Please enter the second number:"))
c = eval(input("Please enter the third number:"))

'''this bubble is to change the order of three numbers'''
def bubbleSort(arr):
	n = len(arr)
	# optimize code, so if the array is already sorted, it doesn't need
	# to go through the entire process
	swapped = False
	# Traverse through all array elements
	for i in range(n-1):
		# range(n) also work but outer loop will
		# repeat one time more than needed.
		# Last i elements are already in place
		for j in range(0, n-i-1):

			# traverse the array from 0 to n-i-1
			# Swap if the element found is greater
			# than the next element
			if arr[j] > arr[j + 1]:
				swapped = True
				arr[j], arr[j + 1] = arr[j + 1], arr[j]
		
		if not swapped:
			# if we haven't needed to make a single swap, we
			# can just exit the main loop.
			return


if isinstance(a or b or c, float):
    x = float(a)
    y = float(b)
    z = float(c)
    arr = x , y , z
    bubbleSort(arr)
    print("Sorted array is:")
    for i in range(len(arr)):
	    print("%d" % arr[i],"≤", end=" ")


elif isinstance(a and b and c, int):
    w = int(a)
    e = int(b)
    r = int(c)
    arr = w , e , r
    bubbleSort(arr)
    print("Sorted array is:")
    for i in range(len(arr)):
	    print("%d" % arr[i],"≤", end=" ")

  
else :
    isinstance(a or b or c, (str)) is True 
    print("Please enter 3 NUMBERS")
 
 
#this the testing git change
